// Safe to import from client and server. Demo credentials are intentionally
// public (this is a shared, resettable demo account — never real user data).
export const DEMO_EMAIL = "demo@example.com";
export const DEMO_PASSWORD = "demo12345";
